I am the founder of APOL AGENT. We are doing a clean-slate restoration of the engine. Do NOT use previous logic versions; follow these EXACT technical specifications to fix Wallet Balances, Activity Logs, Scan Timeouts, and Brand Protection.

1. BRAND PROTECTION (Strict Identity):
- Hardcode the Official APOL CA: 0x0d521b604a25c2825b7131acf243f4b296c64aab
- Hardcode Official Twitter: @ApolAgent_
- IDENTITY LOGIC: If a token is named 'APOL' or 'APOL AGENT', it is only 'Official' if the address matches the hardcoded CA. If the name matches but the address is different, flag it as '⚠️ POTENTIAL CLONE / SCAM' and set Risk to HIGH.

2. ACCURATE WALLET BALANCE (Alchemy-First):
- The 'Current Balance' must use a direct 'eth_getBalance' call via ALCHEMY_BASE_URL. 
- Do NOT rely on indexers for the live balance. Ensure it reflects the real-time ETH balance (e.g., 0.0426 ETH).

3. RESTORE ACTIVITY MATH (Manual Parsing):
- Fetch the 'txlist' from Blockscout or Alchemy for the wallet.
- Implement a loop to calculate:
   * INFLOW: Sum of all 'value' where the 'to' address is the scanned wallet.
   * OUTFLOW: Sum of all 'value' where the 'from' address is the scanned wallet.
   * LEVEL: >50 txs = 'High', 10-50 = 'Moderate', <10 = 'Low'.
- UI format MUST be:
   📊 ACTIVITY (Base Mainnet)
   Transactions: [count] txs
   Level: [Level]
   Inflow: [Inflow] ETH   Outflow: [Outflow] ETH

4. FIX SCAN TIMEOUTS (Optimization):
- Parallelize ALL forensic calls using 'Promise.all' (Balance, Genesis, Funding, Activity).
- Set a strict 15-second timeout for the Activity fetch. If it hits 15s, fallback to 'Scanning (High Activity)' but do NOT let the entire command time out.

5. ENGINE GUARDRAILS:
- Do NOT touch or modify the existing Token Forensic logic, Uniswap V4 detection, or Virtuals Protocol (ERC-8183) logic. These are working perfectly.
- Strictly prioritize ALCHEMY_BASE_URL as the primary RPC.

Show me the code for the Inflow/Outflow summation loop before you apply these changes.