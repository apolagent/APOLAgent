services:
  - type: web
    name: apol-agent
    runtime: node
    buildCommand: npm ci --include=dev && ./node_modules/.bin/tsx script/build.ts
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
