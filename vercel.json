{
  "version": 2,
  "builds": [
    {
      "src": "server/vercel.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/public/**", "server/**"],
        "bundle": false
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server/vercel.ts" }
  ]
}
